<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dayoneapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DayOneApp">
        <activity
            android:name=".EvenAndOdd"
            android:exported="false" />
        <activity
            android:name=".ImageAndOnClickListenerActivity"
            android:exported="false"
            android:label="@string/title_activity_image_and_on_click_listener"
            android:theme="@style/Theme.DayOneApp.NoActionBar" />
        <activity
            android:name=".ActivityMain"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>